public class Rectangle {
    int calcArea(int w, int h) {
        return w*h;
    }

    int calcPer(int w, int h) {
        return (w*2)+(h*2);
    }

    double calcArea(double w, double h) {
        return w*h;
    }

    double calcPer(double w, double h) {
        return (w*2)+(h*2);
    }

}